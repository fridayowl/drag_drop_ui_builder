import { stylefieldslice } from './../slice/stylefield';

const { actions: slice } = stylefieldslice;

export const getfontSize = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getFontFamily = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getFontWeight = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const gettext_align = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getcolor = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getbackground_color = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const gettext_decoration = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getline_height = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getfont_style = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getBorder = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getBorderRadius = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}

export const getTextTransformation = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}

export const getMargin = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}
export const getPadding = (data) => (dispatch) => {
    dispatch(slice.fontSize(data))
}



